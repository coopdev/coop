<?php
   if ($this->sylText) {
      $coopSess = new Zend_Session_Namespace('coop');
      $role = $coopSess->role;
      $sylText = $this->sylText;
      $classId = $this->classId;
      
      echo "$sylText";

      if ($role == 'coordinator') {
         $editUrl = $this->url(array('action'=>'edit'));
         echo "<a class='regText' href='$editUrl?id=$classId'>Edit</a>";
      }
   }
   
?>
