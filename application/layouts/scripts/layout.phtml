<?php 
   $coopSess = new Zend_Session_Namespace('coop');
   $this->headTitle('Cooperative Education');
   $this->headTitle()->setSeparator(' - ');
   $this->headTitle($this->pageTitle);
   
   echo $this->doctype();
?>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
   <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
   <?php 
      echo $this->headTitle();
      $coopSess = new Zend_Session_Namespace('coop');
      //echo $this->headTitle($this->pageTitle);
      $this->headLink()->appendStylesheet($coopSess->baseUrl.'/css/styles.css'); 
      echo $this->headLink();
   ?>
</head>
<body>
   <?php
      echo "<div id=main>";
      
      echo "<div id=innerMain>";
      
      //if ($coopSess->role != 'none') {
         
         //echo "<a class='logout' href='".$this->url(array('controller'=>'auth','action'=>'logout'))."'>Logout </a>";
         
         
      //}
      // Start Header.
      echo "<div id='header'>";
      echo    "<h2> Header </h2>";
      
      echo "</div>";
      // End Header.
      
      // Start main nav
      echo "<div id='mainNav'>";
      echo    "<a href='".$this->url(array('controller'=>'syllabus','action'=>'listall'))."'> Class syllabuses </a> <br />";
      echo    "<a href='".$this->url(array('controller'=>'contract','action'=>'new'))."'> Co-op Agreement Form </a> <br />";
      echo    "<a href='".$this->url(array('controller'=>'user','action'=>'new'))."'> Student Information Sheet </a>";

      echo "</div>";
      // End main nav
      
      // Start content
      echo "<div id=content>";
      echo "<h2>$this->pageTitle</h2>";
           
      
      echo $this->layout()->content;
      
      echo "</div>";
      // End content
      
      echo "<div id='footer'>";
      echo "<h1> Footer </h1>";
      echo "<div>";
      
      echo "</div>";
      // End inner main div
      
      echo "</div>";
      // End main div
   ?>
</body>
</html>

  